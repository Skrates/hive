# Hive edge — cx43 (coolify-fsn1), /home/hive/.config/hive/edge.env (mode 0600, owner hive)
# Seat: talos (Grok Build, SuperGrok subscription). Names only — values live on the box.
# NOTE: EnvironmentFile values are literal — absolute paths only, no %h/~ expansion.
#
# PATH: set only if node is not at /usr/bin/node on this host (prepend its bin dir).
# PATH=
HIVE_BROKER_URL=
HIVE_EDGE_ID=cx43
HIVE_EDGE_TOKEN=
